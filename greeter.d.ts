declare class Student {
    private fullName;
    constructor(firstName: string, middleName: string, lastName: string, suffix?: string);
    greeter(): string;
}
declare let user0: Student;
declare let user1: Student;
